<form method="post" class="" action="{{ path('app_panel_delete_home', {'id': panel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panel.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
